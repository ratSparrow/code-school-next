export const whatYouWillLearnSlick = {

    infinite: true,            // Loop through all 10 elements
    slidesToShow: 10,           // Show only one item at a time
    slidesToScroll: 1,         // Scroll one item at a time
    autoplay: true,            // Enable autoplay
    autoplaySpeed: 500,       // Move every 2 seconds
    speed: 500,                // Animation speed
    fade: true,                // Optional: fade instead of sliding
    arrows: false,             // Hide arrows (optional)
    dots: true                 // Show navigation dots (optional)
};
